node_modules/
dist/
*.tgz
.DS_Store
.next/
next-env.d.ts

# …except the frozen build snapshot, which is committed on purpose: it is the
# only bundle-cost coverage that survives a clean checkout (fixtures/next-demo
# has a real .next/, but it is ignored, so its tests skip in CI).
!fixtures/frozen-build/.next/
!fixtures/frozen-build/.next/**

# The LinkedIn cut. demo.gif is committed (the README embeds it); the mp4 is a
# 1 MB upload artifact that scripts/make-demo regenerates byte-for-byte on demand.
assets/demo.mp4
scripts/make-demo/.work/
