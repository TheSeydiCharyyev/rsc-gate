'use client';

export function Client(_props: {
  thing: unknown;
  sym: symbol;
  when: Date;
  lookup: Map<string, number>;
  label: string;
  count: number;
  config: Record<string, unknown>;
}) {
  return null;
}
